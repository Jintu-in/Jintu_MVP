# Keep transitive deps out of the root — surfaces missing dependencies early
# instead of letting them resolve by accident through hoisting.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
